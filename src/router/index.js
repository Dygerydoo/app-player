import Vue from 'vue';
import Router from 'vue-router';
// import PlayerBar from '@/components/PlayerBar';

Vue.use(Router);

export default new Router({
  routes: [
    // {
    //   path: '/',
    //   name: 'PlayerBar',
    //   component: PlayerBar,
    // },
  ],
});
